// objetos são estruturas de dados que permitem armazenar múltiplos valores em uma única variável. 
// Eles são compostos por pares de chave-valor, onde cada chave é única e está associada a um valor.

let exeObj = {
    nr: 500,
    str: "palavra"
}

console.log(exeObj.nr); // valor 500
console.log(exeObj.str); // valor "palavra"

// criando um objeto 
data = new Data();
let livro = new Object();
// nomeObjeto = new contrutor(argumento);
